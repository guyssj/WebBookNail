import { MinToTimePipe } from './min-to-time.pipe';

describe('MinToTimePipe', () => {
  it('create an instance', () => {
    const pipe = new MinToTimePipe();
    expect(pipe).toBeTruthy();
  });
});
